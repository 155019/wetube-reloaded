header
    div.header__icon
        button#asideBtn
            i.fas.fa-bars
        a(href="/").header__logo
            i.fab.fa-youtube
    nav
        ul
            li
                form(method="GET", action="/search").search__form
                    input(placeholder="검색", name="keyword", type="text")
                    button
                        i.fas.fa-search
            if loggedIn
                li
                    a(href="/videos/upload")
                        i.fas.fa-video
                li
                    a(href="/users/edit")
                        i.fas.fa-user
                li
                    a(href="/users/logout") 로그아웃
                li
                    a(href=`/users/${loggedInUser._id}`)
                        if loggedInUser.avatarUrl === ""
                            span 😀
                        else
                            if loggedInUser.avatarUrl
                                if loggedInUser.avatarUrl.includes("github")
                                    img.header__avatar(src=loggedInUser.avatarUrl, crossorigin)
                                else
                                    img.header__avatar(src=loggedInUser.avatarUrl, crossorigin)
            else
                li  
                    a(href="/login") 로그인
                li
                    a(href="/join") 가입